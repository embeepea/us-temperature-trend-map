// To run the application yourself, make a copy of this file
// named `map-config.js`, and edit the settings below appropriately.

var mapConfig = {
    // Insert your mapbox token here here.  You will need to get your own Mapbox access token;
    // www.mapbox.com for details:
    mapboxToken: "YOUR MAPBOX ACCESS TOKEN HERE",

    // The following URL is the location where the program downloads the daily data for each
    // station.  If you change this, be sure it does NOT end with a "/":
    ghcndDataURLPrefix: "https://s3.amazonaws.com/nemac-ghcnd"
};
